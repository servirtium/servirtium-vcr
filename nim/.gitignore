nimcache/
# compiled test binaries (Nim emits the executable next to the .nim source)
tests/playback
tests/*_test
*.exe
# native engine .so — staged fresh by nim/.package.ae; consumer_example binary
native/*.so
native/*.dylib
native/*.dll
*consumer_example

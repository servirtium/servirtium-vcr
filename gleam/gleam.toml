name = "servirtium_gleam"
version = "2.0.0"
target = "erlang"

description = "Record/replay HTTP service tests (Servirtium markdown tapes) for Gleam — over the Aether VCR core's C NIF on the BEAM."
licences = ["MIT"]

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"

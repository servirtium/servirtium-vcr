[pytest]
# The Aether VCR is ONE active server per process in v1 (its tape/cursor/
# mutation state is process-global), so tests MUST run serially. pytest does
# this by default — do NOT add pytest-xdist (-n) parallelism.
testpaths = test
addopts = -ra

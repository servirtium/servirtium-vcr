__pycache__/
*.pyc

# virtualenvs
.venv/
venv/

# packaging
servirtium.egg-info/
build/
dist/

# native build artifacts — staged fresh by python/.package.ae (copies the
# engine .so from core/native/ into the package tree before `python -m build`).
# Build-only; never commit the binary.
servirtium/native/*.so
servirtium/native/*.dylib
servirtium/native/*.dll

# editor / IDE
.idea/
*.iml

# scratch tapes
test/just_testing_ignore_me.md

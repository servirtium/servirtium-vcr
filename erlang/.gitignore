# Shared NIF build output (the OTP app consumed by Elixir + Gleam via ERL_LIBS).
_build/

# Legacy in-place build artifacts.
ebin/

# Native build artifacts: the compiled C NIF (links the engine via rpath).
priv/*.so
priv/*.dylib
priv/*.dll

# If the VM crashes, it generates a dump.
erl_crash.dump

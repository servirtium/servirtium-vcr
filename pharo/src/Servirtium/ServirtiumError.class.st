"
Raised when the VCR fails to start, a mutation call reports an error, or a
record-mode flush detects drift (failIfChanged).
"
Class {
	#name : 'ServirtiumError',
	#superclass : 'Error',
	#category : 'Servirtium',
	#package : 'Servirtium'
}

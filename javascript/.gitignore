node_modules
.vscode
.idea
dist

# Native libraries are downloaded or built, then staged by the package step.
# Never commit platform binaries.
native/*.dylib
native/*.dll

# native libservirtium_vcr.so — staged fresh by javascript/.package.ae, never commit
native/*.so
pkgout/

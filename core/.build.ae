// The shared Servirtium VCR engine, built ONCE for the whole monorepo from
// the engine source that now lives IN this repo (core/), no longer in
// the Aether stdlib.
//
// Output: core/native/libservirtium_vcr.so — every binding's .tests.ae deps
// this node and then links (cgo/NIF/cabal) or loads (ctypes/ffi/koffi/Fiddle/
// FFM/uFFI, via SERVIRTIUM_VCR_LIB) that single artifact.
//
// Source: core/embed.ae (the aether_vcr_embed_* C-ABI) imports the local
// `vcr` module (core/vcr.ae — the Servirtium markdown parser /
// emitter / dispatcher-registration), whose externs are backed by the
// hand-written core/aether_vcr.c (storage + dispatcher + record),
// linked via `--extra`. It still builds against the Aether toolchain + runtime
// (the engine uses std.http's server, std.fs, std.string) — Aether stays a
// build dependency; only the VCR-specific source is now monorepo-owned.
//
// Needs ae >= 0.182 (-fPIC runtime for --emit=lib --with=net); chunked
// de-chunk >= 0.183.

import build
import std.os

main() {
    b = build.start()
    root = build._get(b, "root")
    dest = "${root}/core/native"
    _m = os.system("mkdir -p \"${dest}\"")
    rc = os.system("cd \"${root}/core\" && ae build --emit=lib --with=fs,net embed.ae --extra aether_vcr.c -o \"${dest}/libservirtium_vcr.so\"")
    if rc != 0 {
        println("engine: failed to build libservirtium_vcr.so from core (is `ae` >= 0.182 installed?)")
        return 1
    }
    println("engine: built core/native/libservirtium_vcr.so (from core)")
    return 0
}
